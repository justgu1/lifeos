// Scaffold shell. Routing and providers (TanStack Query, Zustand) are wired
// per work-item; the Home (check-in) screen is CHECKIN-002.

export function App() {
  return (
    <main className="min-h-dvh grid place-items-center">
      <h1 className="text-xl font-semibold">LifeOS</h1>
    </main>
  );
}
