import { expect, test } from "@playwright/test";

// Scaffold smoke test. Real flows (setup wizard, daily check-in) are added
// with their work-items.
test("scaffold placeholder", async () => {
  expect(true).toBe(true);
});
