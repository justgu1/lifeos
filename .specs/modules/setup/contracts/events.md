# EVENTS

## PUBLISHED

None of its own. Setup owns no aggregate and publishes no domain events. It emits the strategy, routine, and planning creation events transactionally through platform: vision.created, goal.created, block-definition.created, scheduled-block.added, routine.created, cycle.created, and the first week.started.

## CONSUMED

None.
