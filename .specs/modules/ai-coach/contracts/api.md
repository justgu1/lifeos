# API

## GET /coach

Deferred — this endpoint is not yet implemented and ships only as a scaffold. The intended response returns the generated suggestions:

response:

```json
{
  "suggestions": [
    {
      "id": "uuid",
      "generated_at": "timestamp",
      "context_ref": "string",
      "text": "string",
      "signals": {}
    }
  ]
}
```
